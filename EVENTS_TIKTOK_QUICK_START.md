# 🚀 Быстрый старт: TikTok-стиль страницы /events

## 📋 Краткое описание

Вертикальная лента событий в стиле TikTok с:

- Одно событие на экран
- Вертикальный snap-scroll
- Динамический фон по тематике
- Анимированные объекты на фоне
- Кнопка "Показать похожее"

---

## 🏗️ Архитектура (кратко)

```
EventsTikTokFeed (главный компонент)
├── EventsTikTokContainer (snap-scroll контейнер)
│   ├── DynamicBackground (динамический фон)
│   │   └── AnimatedObjects (планеты/иконки)
│   ├── EventSlide[] (каждое событие в отдельном слайде)
│   │   └── EventCard (существующий компонент)
│   └── SimilarEventsButton (фильтр по тематике)
```

---

## 🎯 Ключевые технологии

- **CSS Scroll Snap** - для привязки к слайдам
- **Intersection Observer** - для определения активного слайда
- **Framer Motion** - для плавных переходов фона
- **React Query (useInfiniteQuery)** - для бесконечной загрузки
- **Lucide React** - для иконок анимированных объектов

---

## 📦 Основные компоненты

### 1. EventsTikTokContainer

- Контейнер с `scroll-snap-type: y mandatory`
- Каждый слайд: `height: 100vh`, `scroll-snap-align: start`

### 2. DynamicBackground

- `position: fixed`, покрывает весь экран
- Меняет градиент на основе `getPastelTheme(theme)`
- Плавные переходы через Framer Motion

### 3. AnimatedObjects

- 3-5 объектов в зависимости от тематики
- Плавные движения по траектории
- Опциональное вращение

### 4. EventSlide

- Обертка высотой `100vh`
- Использует Intersection Observer для определения активности
- Содержит `EventCard`

### 5. SimilarEventsButton

- Фиксированная кнопка в правом нижнем углу
- Фильтрует события по текущей тематике
- Показывает активный фильтр

---

## 🔄 Логика работы

1. **Загрузка данных**: `useInfiniteQuery` с пагинацией
2. **Определение активного слайда**: Intersection Observer следит за видимостью
3. **Смена фона**: При смене `currentIndex` меняется `currentTheme` → обновляется `DynamicBackground`
4. **Предзагрузка**: Когда до конца остается 3 события, загружается следующая страница
5. **Фильтрация**: При клике на "Показать похожее" фильтруются события по тематике

---

## 💡 Основные паттерны

### Snap Scroll

```scss
.container {
  scroll-snap-type: y mandatory;
}

.slide {
  scroll-snap-align: start;
  height: 100vh;
}
```

### Плавная смена фона

```typescript
<motion.div
  animate={{ background: newBackground }}
  transition={{ duration: 0.8, ease: 'easeInOut' }}
/>
```

### Определение активного слайда

```typescript
const observer = new IntersectionObserver(
  ([entry]) => {
    if (entry.isIntersecting && entry.intersectionRatio > 0.5) {
      setCurrentIndex(index);
    }
  },
  { threshold: 0.5 },
);
```

---

## 🎨 Анимированные объекты

### Структура объекта

```typescript
{
  icon: <Music />,
  initialPosition: { x: '10%', y: '20%' },
  movementRange: { x: 50, y: 100 },
  duration: 25,
  size: 60,
  rotation: true
}
```

### Анимация

```typescript
<motion.div
  animate={{
    x: movementRange.x,
    y: movementRange.y,
    rotate: rotation ? 360 : 0,
  }}
  transition={{
    duration: duration,
    repeat: Infinity,
    ease: 'linear',
  }}
/>
```

---

## ⚡ Оптимизация

1. **Мемоизация**: `React.memo` для EventSlide
2. **Lazy loading**: Изображения через `next/image`
3. **GPU-ускорение**: `will-change: transform` для объектов
4. **Предзагрузка**: Загружаем следующую страницу заранее
5. **Уважение к prefers-reduced-motion**: Отключаем анимации при необходимости

---

## 📱 Мобильная поддержка

- Touch события для swipe (опционально)
- `-webkit-overflow-scrolling: touch`
- Адаптивные размеры кнопок
- Оптимизация производительности

---

## 🔗 Связанные документы

- **EVENTS_TIKTOK_ARCHITECTURE.md** - Детальное архитектурное описание
- **EVENTS_TIKTOK_IMPLEMENTATION_EXAMPLES.md** - Полные примеры кода

---

## ✅ Чеклист реализации

- [ ] Создать структуру папок компонентов
- [ ] Реализовать EventsTikTokContainer с snap-scroll
- [ ] Создать DynamicBackground с поддержкой тем
- [ ] Реализовать AnimatedObjects для основных тем
- [ ] Создать EventSlide с Intersection Observer
- [ ] Добавить SimilarEventsButton
- [ ] Интегрировать useEventsFeed хук
- [ ] Тестировать на десктопе и мобильных
- [ ] Оптимизировать производительность
- [ ] Добавить обработку ошибок и пустых состояний

---

## 🎯 Приоритеты

1. **Фаза 1**: Базовая структура + snap-scroll + загрузка данных
2. **Фаза 2**: Динамический фон + базовые анимации
3. **Фаза 3**: Анимированные объекты + кнопка фильтра
4. **Фаза 4**: Полировка + оптимизация
